// Enum for export types used in file generation logic
export const EXPORT_TYPES = {
  PDF: "pdf",
  EXCEL: "excel",
};

// Derived array of allowed export type values
export const EXPORT_TYPE_VALUES = Object.values(EXPORT_TYPES);

// Enum for expense categories
export const CATEGORIES = {
  NEEDS: "Needs",
  WANTS: "Wants"
};

// Derived array of allowed category values
export const CATEGORIES_VALUES = Object.values(CATEGORIES);
